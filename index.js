// what is package.json?
// package.json file holds a detail reagrding your coding like project versions, dependency, how much packages and commands
// package-lock.json file manages the information related to your packages
// To create a package.json file :- npm init 
// Node.JS is a single threaded not a multithreaded -->> Interveiw Question
// If you deleted node_modules folder mistakenly then simply run the command "npm install"
// We shouldn't push the node_modules folder to the git bc of storage size 

const colors = require("colors");
console.log("Package.json".zebra);
